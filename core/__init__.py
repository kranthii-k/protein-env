"""
core — Shared business-logic package for the ProteinEnv OpenEnv environment.

Contains the environment state machine, episode management, tool dispatch, and
the ESM2 embedding client. Imported by both server/ (at runtime) and tests/.
"""
