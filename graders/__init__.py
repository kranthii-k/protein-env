"""
graders — Pure-function reward-calculation package for ProteinEnv.

All public symbols exported by this package must be pure functions: they accept
typed inputs and return typed outputs with no side effects and no mutable state.
This constraint is enforced by the test suite (≥ 80 % branch coverage required).
"""
